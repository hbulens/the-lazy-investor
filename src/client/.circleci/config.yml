version: 2.1
jobs:
  build:
    docker:
      - image: circleci/node:latest
    steps:
      - checkout
      - node/with-cache:
          steps:
            - run:
                name: Install
                command: npm install
      - run:
          name: Run tests
          command: npm test
