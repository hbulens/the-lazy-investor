version: 2.1
jobs:
  build:
    working_directory: ~/src/client
    docker:
      - image: circleci/node:latest
    steps:
      - checkout
      - node/with-cache:
          steps:
            - run:
                name: Install
                command: npm install
      - run:
          name: Run tests
          command: npm test
